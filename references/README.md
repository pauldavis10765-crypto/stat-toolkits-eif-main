# References workspace

This folder is reserved for local source PDFs, notes, converted text, and other
private reference material.

Only this `README.md` should be tracked. The project should contain
independently written summaries, derivations, examples, and citations rather
than copied source files.
